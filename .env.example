# ── Local mode (ollama via ollama-bridge) ────────────────────────────────────
# Set these to run locally. OPENAI_API_KEY must match BRIDGE_API_KEY in
# ollama-bridge/.env. Leave blank to use hosted (Hugging Face) mode instead.
OPENAI_API_KEY=test